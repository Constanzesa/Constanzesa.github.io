<article class="post">
  <header class="post-header">
    <h1>{{ title }}</h1>
    <p class="post-meta">{{ date | date("yyyy-MM-dd") }} — by {{ site.author }}</p>
  </header>

  <div class="post-body">
    {{ content | safe }}
  </div>
</article>
